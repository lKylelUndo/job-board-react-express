type ProfileTypes = {
  username: string;
  location: string;
  position: string;
  aboutMe: string;
  experience: string;
  skills: string;
  resume: string;
};

export const ManageProfile = (profileData : ProfileTypes) => {
  try {
    
  } catch (error) {
    console.log(error);
    return null;
  }
};
